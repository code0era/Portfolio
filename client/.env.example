# Client environment variables
# For local development, set VITE_API_URL to your local server
# For production on Vercel, VITE_API_URL should be empty (uses relative /api path)
VITE_API_URL=http://localhost:5000
